info:
  name: POST inicio sesion
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/auth/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email":"ana@example.com",
        "password":"Ana12345!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
