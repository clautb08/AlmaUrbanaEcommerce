info:
  name: GET service
  type: http

http:
  method: GET
  url: http://localhost:3000/api/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
