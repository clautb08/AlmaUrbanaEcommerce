info:
  name: GET user
  type: http

http:
  method: GET
  url: http://localhost:3000/api/user
  headers:
    - name: Authorization
      value: Bearer valid-user-token
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
